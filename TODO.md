# TACO ERP: TBO-01

작성일: 2026-06-26 (금) · 작성: 개발자 (김민수)

## 1. 오늘의 요약

학원 백오피스 ERP(TACO)의 **프론트엔드 전 기능 1차 구현**과 **백엔드 API·문서화**를 진행했습니다.
세 개의 독립 저장소(프론트 / 백엔드 / 공유 계약)로 구성했고, 화면은 디자인 시스템 위에서 동작하며 모든 코드가 타입체크를 통과합니다.

## 2. 완료한 작업

### 설계 · 기반

- ERD 확정(`docs/erd.dbml`) — 학생/부모·상담·수업·입출금·보고서/알림.
- 공유 계약 패키지 **@kms545487/contracts** 도입 → 프론트/백 도메인 타입 단일 소스(불일치 원천 차단).
- 디자인 시스템(GitHub 느낌, 밝은 테마) + 좌측 내비/상단바 셸.

### 화면 (프론트엔드)

- **대시보드** — 권한별 분기. 대표(CEO) 전용 **경영지표 + 매출 차트**(기간/인당/과목/수업, chart.js).
- **학원 캘린더** — 수업+이벤트 통합, 필터. 학생·학부모 기본=내 수업+중요 이벤트.
- **상담** — 캘린더(상태별 색)·신청 폼·상세 편집(회차 누적).
- **학생** — 등록(학생/학부모 Web ID 검증·코스)·검색·삭제(연관 데이터 cascade).
- **수업(강사)** — 개설(단일/기간·반복, 주제 라벨 추천)·검색·출석·**피드백 작성(상세 폼, 학부모 연결)**.
- **결제·수납** — 리스트/캘린더·청구·수정·수납.
- **강사 페이** — 시수×시급 산정 + **진행 수업 내역**·요청→승인→지급.
- **지출·비품** — 분류·**영수증 첨부**·요청.
- **관리자** — 코스/과목 추가, 이벤트 발행(우선순위), **대표 승인 센터**.

### 권한 · 워크플로우

- 역할(RBAC): 대표만 경영지표·승인, 매니저/강사 운영, 학생/학부모 일정.
- 승인: **강사 페이·지출은 대표(super_admin) 승인 후** 출금 반영.

### 백엔드 (NestJS, in-memory)

- 모듈: 인증(JWT)·헬스·계정(web id 검증)·학생·수강·결제·과목·코스·지출.
- **Swagger API 문서 `/docs`** (현재 20개 엔드포인트).

### 품질

- 프론트/백 **타입체크 통과**, mock 데이터 **참조 무결성 0건**.
- REST 클라이언트 Axios 전환, 하이드레이션/날짜 버그 수정.

## 3. 진행 중 / 다음 단계

- 백엔드 도메인 추가: 수업·출석·피드백·상담·강사정산·이벤트(동일 패턴, sessions·counsel 우선 설계).
- 인증/권한을 실제 JWT(roles 클레임)와 연결(현재 데모 전환 스위치).
- mock 스토어 → 백엔드 API 본격 연동, 이후 PostgreSQL(TypeORM) 이관.
- 토글(지출 분류·과목 등) 관리자 추가 → **옵션 세트(룩업) 테이블** 도입 제안.
- 수업 피드백 → 학부모 카카오 발송(MQ) 연결.

## 4. 비고

- 데모 실행: `cd backend && npm run dev`(:3001, 문서 /docs) + `cd frontend && npm run dev`(:3000).
- 저장소 3개 모두 상세 커밋 이력 유지.

---

# TACO ERP: TBO-02

작성일: 2026-06-29 (월) · 작성: 개발자 (김민수)

> 목표: 아래의 TODO-List를 구현하는 것을 목표로 하나, Front의 UI/UX부터 구조화 및 최적화 하는 것을 목표로 함. 오늘부터는 개발이 어느정도 완료된 기능의 test-code와 상세 커밋, join시 관계 의존성 체크 및 강건성 재고, Backend와의 호환 및 확장 가능성을 목표함.

## 1. 상세 페이지

> 목록이 있는 탭에서 목록 안의 데이터가 많아지면 어떻게 최적화 및 UX 개선을 할 것인지 계획 필요함. page button, filtering, searching 등

> 모든 탭에서 캘린더, 신청 폼, 목록 등이 함께 있는데 조금 더 UI/UX적으로 예뻤으면 좋겠음. 캘린더는 일정 관련해서 페이지 예쁘게, 신청 폼은 신청 폼대로, 목록과 관리는 그것들 끼리 모아서 보여줘야함. (대신 상세한 정보가 한 페이지에 한 주제로 들어가게)
> 슬슬 front가 커지면서 Join이 많아지고 있음. 모든 작업에서 join 유효성 검사 -> 업데이트 모두 반영 되는지 체크 하고, 문서화를 꼼꼼하게 하는 것을 목표로 함. (백엔드와의 연동이 쉽게, .dbml도 틈틈이 수정해야 함.)

### 상담 탭

1. 상담 신청 폼을 상세 페이지로 빼면 좋겠음. (디자인을 더 예쁘게 설문지처럼 만들기 위함)
2. 상담탭에서는 대략적인 스케줄과 클릭시 상세 상담 및 예약 내역, 예약 신청, 스케줄 변경 등을 할 수 있도록 유도
3. 캘린더와 상담카드 목록 등에서 학생을 검색하거나, 상담자 이름을 필터링 하는 등의 기능이 있으면 좋겠음. (목록은 백엔드에서 요청할 수 있도록)

### 캘린더 탭

1. 월간 데이터만 보여주고 있으나, 주간 데이터도 볼 수 있어야 함. (구글 캘린더처럼 주별 시간별.)
2. 수정하기 기능을 추가하여 실제 캘린더처럼 커스텀 반복(~이후로 반복, ~까지 반복, 무슨 요일만 반복 등)과 시간 수정이 가능해야 함
   (ex: 월요일 12시부터 14시까지였던 수업을 월요일 14시부터 16시로 옮김 -> 관련 조인 계정 전부 업데이트 -> 반복 여부 물어봄 -> PNG 파일로 다운로드 가능 -> URL로 공유하기 가능)
3. 드래그를 통한 이동, 클릭하여 해당 스케줄을 모달로 띄운 뒤 시간 및 반복 횟수 등 상세 수정 가능하게

### 학생 부모 탭

1. 학생 목록에서 학생 정보 수정 기능이 필요함 (추후 Web ID 추가하기 위함)
2. 학생을 클릭했을 때 상세 페이지가 뜨면 좋겠음. (등록한 코스 -> 클릭시 강의 상세 페이지 -> 강사 상세 페이지 등 유기적인 원형 flow, 학생 상담 카드, 연락처, 특이 사항 메모 등도 함께 보여주면 좋을 듯)
3. 우리의 목적은 학생의 정보를 데이터화 하는 것임. 학생을 삭제하더라도, 상담 내역, 유저 정보, 학점 등은 삭제하면 안됨. 그저 관련된 스케줄 및 Join에서만 삭제해주면 됨 (status != drop 이런 식으로 필터링)

### 수업 강사 탭

1. 강사는 수업 개설 화면이 보이지 않아야 함.
2. 수업 목록에서 캘린더 뷰, 위클리 뷰도 추가해야 함
3. 강사를 위한 탭이므로, 내가 이번 달 몇번 수업을 했고, 얼마를 벌었는지 볼 수 있어야 함.
4. 테이블 컬럼에 진행 회차(12 / 15), 학생 수 등을 보여주면 좋겠음.

### 결제 수납 탭

> 대표님께서 가장 먼저 구현하라고 한 내용임. 꼭 완성될 필요는 없지만 일단 테스트라도 가능하게 front, back test-code + 실제 연결해서 backend console + test.txt에 저장해보는 식으로 구현해보기

1. 결제 수납에서 상태, 금액에 따른 시각화 뷰가 추가되면 좋겠음.
2. 기한/수납일 보다는 등록/수납일로 수업 등록과 수납을 언제 했는지 볼 수 있으면 좋을 것 같음.
3. 해당 탭은 아주 민감한 기능이므로 권한과 보안에 대해 아주 상세히 신경써야 함.

### 강사 페이 탭

1. DB join하여 해당 강사가 어떤 수업을 맡고 있는지 떠야 함
2. 해당 수업을 클릭하면 진행 회차, 시수당 금액 등이 자동으로 계산 됨
3. 다만 강사가 직접 수정을 할 수 있음.
4. join하여 기간, 강사의 코스 목록, 학생 목록, 스케줄 등을 아주 면밀히 연동해야 함 (절대 오류가 있으면 안됨.)

### 지출 비품 탭

1. 캘린더 뷰에서 모든 지출목록이 같은 색임.
2. 지출 분류에 따라 다른 색상으로 표현해야 함
3. 지출 등록이 있으나, 상세로 들어갔을 때 수정도 가능해야 함.

### 수업보고서

> TODO

### 관리자

1. 우선은 결제 및 수납 관련한 기능만
2. 결제 및 수납 체크, 지출 및 비품 승인 등 모든 승인 관련 권한 체크
3. 모든 승인 관련 join과 의존성 체크 후 test-code 작성
4. 개요, 캘린더 뷰에서 날짜를 클릭하면 스케줄 추가 및 수정이 가능해야 함. (이미 존재하는 스케줄도 클릭시 수정 및 삭제 가능)
5. 캘린더 탭에서 요청한 것 처럼 위클리 뷰도 추가 및 수정 삭제가 가능해야 함. (부모님은 위클리 뷰로 보는 것을 선호하기 때문임)
6. 코스추가, 과목 추가, 로드맵 추가가 모두 상세 페이지로 있어야 함. (우선은 지금의 정보로만 구성을 하되, 모달 등을 이용하여 예쁜 UI/UX로 구성해야 함.) -> 추후에 많은 옵션들이 들어갈 예정이라 그럼
7. 로드맵은 코스, 과목, 강사 등이 M:N으로 들어가므로 아주 면밀한 의존성 체크가 필요함. test-code를 작성해야 함
8. 이벤트 발행을 따로 빼기 보다는 캘린더 뷰에서 추가하면 좋을 것 같음. (모달로 띄우던가 상세 페이지를 만들던가)
9. 현재 승인 버튼을 눌러 여러가지의 승인을 한번에 보여주는데, 결국 관리자 페이지가 필요할 것 같음. 일단 로직부터 구현해놓고 추후에 관리자 페이지는 확장 및 분리하는 것으로

---

## 2. 실행 계획 (개발자 제안 · 김민수)

> 핵심 판단: **요구의 대부분은 "새 설계"가 아니라 "계약을 ERD 수준으로 끌어올리는 일"임.**
> `docs/erd.dbml`은 이미 `schedules(start_at/end_at)`, `roadmaps`·`roadmap_courses(M:N)`, `students.status` 소프트삭제 전제, `payments.created_at`(등록일)까지 설계돼 있음.
> 반면 프론트의 `@kms545487/contracts`는 그 **단순화 버전**(예: `ClassSession`에 시간대 없음, 로드맵 없음)이라 TBO-02의 위클리뷰·드래그·로드맵을 **지금 구조로는 만들 수 없음**.
> 따라서 화면부터 손대면 재작업이 큼 → **계약 먼저(0단계) → 공통 기반(1단계) → 탭별(2단계)** 순서를 강하게 권장함.

> 표기: ⚠️=현재 코드와 충돌(반드시 변경) · 🧱=계약/스키마 변경 · 🧪=test-code 필수 · 🔒=권한·보안

### 0단계. 선행 — 계약·기반 (먼저 못 하면 위가 다 막힘)

1. 🧱 **수업에 시간대 추가**: `ClassSession`에 `startTime`(또는 `startAt`) 필드. 종료 = 시작+`durationMinutes`로 파생. → 위클리(시간별) 캘린더·드래그·"월12시→월14시 이동"의 **전제**. (ERD `schedules.start_at/end_at`와 정렬)
2. 🧱 **반복(시리즈) 모델**: 현재 반복 수업은 독립 row 다건이라 **시리즈 편집 불가**. `seriesId` + 반복규칙(요일/종료조건) 도입 → "이 일정만 / 이후 전부 / 전체 수정"(구글 캘린더 3-way) 가능하게.
3. ⚠️🧱 **학생 소프트삭제로 전환**: 지금 `removeStudent`는 출석·피드백·수강·결제·부모연결을 **하드 삭제**함 → TBO-02(학생부모 #3: 상담·유저·학점 보존, 스케줄/Join만 제거)와 **정반대**. `status='dropped'` 처리 + "활성 스코프" 쿼리 헬퍼로 전환하고, 목록은 `status != dropped` 필터. (StudentsView 삭제 확인 문구도 "함께 삭제됩니다" → "수업/일정에서만 제외" 로 수정)
4. 🧱🧪 **로드맵 엔티티 + M:N 조인**: `Roadmap`, `RoadmapCourse`(코스 M:N), 강사 연결까지. ERD에 이미 있음 → 계약·스토어로 끌어오기. 의존성 깨짐 검사 test 필수.
5. 🧱 **결제 등록일 노출**: `Payment.createdAt`(등록일)을 스토어 생성 시 채우고, 화면은 기한/수납일 대신 **등록/수납일** 기준으로 표시(결제수납 #2).
6. 🔒 **권한 정책 중앙화**: 지금은 `isAdmin/isCEO`가 화면마다 산발적. `lib/permissions.ts`에 (역할 × 리소스 × 액션) 매트릭스 + `<Can>` 가드 도입. 특히 결제는 여기서 단일 관리.
7. 🧪 **테스트 러너 도입** (현재 0개): 프론트/도메인 로직 = **Vitest**, 백엔드 e2e = **Jest+supertest**. `lib/mock/integrity.ts`도 스크립트→테스트로 승격. → TBO-02 곳곳의 "test-code" 요구의 **전제 조건**.
8. **erd.dbml 동기화**: 위 0-1~0-5 반영. 이후 매 조인 추가 시 .dbml을 같은 PR에서 갱신(소스 오브 트루스 유지).

### 1단계. 공통 UI/UX 기반 (탭마다 반복 구현 금지 — 1개로 통일)

1. **목록 추상화 1개**: `usePagedList` + `<DataTable>`(페이지 버튼·검색·정렬·필터). 백엔드는 `?page=&size=&q=&sort=` 쿼리 DTO로 대응(목록은 백엔드에서 요청 요구 충족). → 데이터 많아질 때 모든 탭 일괄 적용.
2. **탭 IA 분리 원칙**: 한 페이지 = 한 주제. ① 캘린더(일정) ② 신청/등록 폼은 **상세 페이지**로 분리 ③ 목록·관리는 따로. (현재 StudentsView처럼 폼+목록 한 화면 → 분리)
3. **위클리(시간별) 캘린더 컴포넌트** `WeekCalendar`: 구글 캘린더식 요일×시간 그리드. MonthCalendar와 토글. 클릭→모달, 드래그→이동(0단계 시간필드 위에서 동작).

### 2단계. 탭별 (TBO-02 항목 매핑)

- **상담**: 신청 폼 → 설문지형 상세 페이지 분리 / 일정+상세(예약·변경) / 학생·상담자 검색·필터(백엔드 목록). 
- **캘린더**: 월/주 토글, 일정 클릭→모달 시간·반복 수정(3-way), 드래그 이동 → 관련 Join 전부 갱신. PNG 다운로드·URL 공유는 외부 라이브러리·공개 토큰 라우트 필요 → **후순위(별도 단계)**.
- **학생·부모**: 정보 수정(추후 Web ID 추가용) / 학생 상세 페이지(코스→강의→강사 순환 flow, 상담카드·연락처·메모) / 소프트삭제(0-3).
- **수업(강사)**: 🔒 강사에겐 개설 폼 숨김 / 캘린더·위클리 뷰 / 강사 본인 "이번 달 N회·수입" / 컬럼에 진행회차(12/15)·학생수.
- **결제·수납**: 🔒 상태·금액 시각화 / 등록·수납일 기준 / **권한·보안 최우선** / 🧪 front+back **실연동 파일럿**(콘솔+`test.txt` 기록).
- **강사 페이**: 🧪 강사→코스→세션→학생 Join, 코스 클릭→회차·시수금액 자동계산, **강사 수동 수정 허용**, 기간/목록/스케줄 정합성 test(오류 0 목표).
- **지출·비품**: 분류별 캘린더 **색상 구분**(eventStyle처럼 category 색맵) / 상세 진입 시 **수정** 가능.
- **관리자**: 결제·지출 승인 권한 체크 🧪 / 캘린더 날짜 클릭→스케줄·이벤트 추가·수정·삭제(모달) / 코스·과목·로드맵 **상세 페이지(모달)** / 🧪 로드맵 M:N 의존성 / 승인 로직 먼저 → 관리자 페이지는 추후 분리.

### 3단계. 권한·보안 (결제 민감 — 별도 강조)

- `permissions.ts` 매트릭스로 결제/승인/페이 접근을 역할 단위로 차단(강사·학생은 결제 탭 진입 불가 등).
- 🧪 승인 워크플로우(지출·페이·결제)의 **의존성·상태전이** 테스트(요청→승인→출금 외 경로 차단).

### 4단계. 테스트 전략 (요구된 "test-code" 총정리)

- Join 유효성: 학생 소프트삭제 후 상담/학점 보존 & 스케줄 제외 검증.
- 승인 의존성: 미승인 출금 불가, 중복 승인 방지.
- 로드맵 M:N: 코스/강사 삭제 시 조인 정합성.
- 결제 파이프라인: front 입력→back 저장→콘솔/`test.txt` 라운드트립(대표 1순위 기능을 **통합 파일럿**으로).

---

## 3. 보완·추가 제안 (적극 어필)

1. ⚠️ **하드삭제 충돌이 가장 시급**: `removeStudent` 현 동작이 TBO-02 의도와 정면 충돌. 0-3을 다른 작업보다 먼저 처리하지 않으면 데모 중 데이터가 영구 소실됨.
2. 🧱 **시간 필드 없이는 캘린더 요구 전부 불가**: 위클리·드래그·시간수정·반복이동은 0-1 없이는 착수 자체가 막힘. **계약 PR을 1번으로** 권장.
3. **타임존 고정(KST) 결정 필요**: 시간대 캘린더 도입 시 과거 하이드레이션 버그처럼 로컬/UTC 혼선 위험. ISO 저장+표시 모두 KST로 결정론적 처리.
4. **감사 로그(audit) 권장**: 결제·승인·스케줄 이동처럼 민감/되돌리기 어려운 액션은 "누가·언제·무엇을"을 남겨야 분쟁·디버깅에 대응 가능. (`Audited`만으로는 행위 이력 부족)
5. **결제 탭을 통합 파일럿으로**: 대표 1순위 + 테스트 파일럿을 한 번에. 다른 탭은 mock 유지하되 결제만 front↔back 실연동으로 끝까지 뚫어 패턴을 확정하고 나머지에 복제.
6. **PNG/URL 공유는 분리**: html-to-image(캡처) + 공개 읽기전용 토큰 라우트가 필요해 범위가 큼. 캘린더 코어 안정화 후 별도 단계로.
7. **로드맵은 ERD에 이미 있음**: 새로 설계하지 말고 `roadmaps`/`roadmap_courses`를 계약으로 가져오면 됨(중복설계 방지).
8. **위클리 뷰 우선순위 ↑**: 학부모가 주간뷰 선호라고 명시 → 캘린더 단계에서 주간을 월간과 동급 1순위로.

### 권장 진행 순서 (스프린트)

`0단계(계약·테스트러너) → 결제 파일럿(실연동+test) → 학생 소프트삭제 → 공통 목록/DataTable → 캘린더(월/주·모달·드래그) → 로드맵 M:N → 나머지 탭 시각화·상세화 → PNG/URL 공유`
