# TACO ERP: TBO-01

작성일: 2026-06-26 (금) · 작성: 개발자 (김민수)

## 1. 오늘의 요약

학원 백오피스 ERP(TACO)의 **프론트엔드 전 기능 1차 구현**과 **백엔드 API·문서화**를 진행했습니다.
세 개의 독립 저장소(프론트 / 백엔드 / 공유 계약)로 구성했고, 화면은 디자인 시스템 위에서 동작하며 모든 코드가 타입체크를 통과합니다.

## 2. 완료한 작업

### 설계 · 기반

- ERD 확정(`docs/erd.dbml`) — 학생/부모·상담·수업·입출금·보고서/알림.
- 공유 계약 패키지 **@kms545487/contracts** 도입 → 프론트/백 도메인 타입 단일 소스(불일치 원천 차단).
- 디자인 시스템(GitHub 느낌, 밝은 테마) + 좌측 내비/상단바 셸.

### 화면 (프론트엔드)

- **대시보드** — 권한별 분기. 대표(CEO) 전용 **경영지표 + 매출 차트**(기간/인당/과목/수업, chart.js).
- **학원 캘린더** — 수업+이벤트 통합, 필터. 학생·학부모 기본=내 수업+중요 이벤트.
- **상담** — 캘린더(상태별 색)·신청 폼·상세 편집(회차 누적).
- **학생** — 등록(학생/학부모 Web ID 검증·코스)·검색·삭제(연관 데이터 cascade).
- **수업(강사)** — 개설(단일/기간·반복, 주제 라벨 추천)·검색·출석·**피드백 작성(상세 폼, 학부모 연결)**.
- **결제·수납** — 리스트/캘린더·청구·수정·수납.
- **강사 페이** — 시수×시급 산정 + **진행 수업 내역**·요청→승인→지급.
- **지출·비품** — 분류·**영수증 첨부**·요청.
- **관리자** — 코스/과목 추가, 이벤트 발행(우선순위), **대표 승인 센터**.

### 권한 · 워크플로우

- 역할(RBAC): 대표만 경영지표·승인, 매니저/강사 운영, 학생/학부모 일정.
- 승인: **강사 페이·지출은 대표(super_admin) 승인 후** 출금 반영.

### 백엔드 (NestJS, in-memory)

- 모듈: 인증(JWT)·헬스·계정(web id 검증)·학생·수강·결제·과목·코스·지출.
- **Swagger API 문서 `/docs`** (현재 20개 엔드포인트).

### 품질

- 프론트/백 **타입체크 통과**, mock 데이터 **참조 무결성 0건**.
- REST 클라이언트 Axios 전환, 하이드레이션/날짜 버그 수정.

## 3. 진행 중 / 다음 단계

- 백엔드 도메인 추가: 수업·출석·피드백·상담·강사정산·이벤트(동일 패턴, sessions·counsel 우선 설계).
- 인증/권한을 실제 JWT(roles 클레임)와 연결(현재 데모 전환 스위치).
- mock 스토어 → 백엔드 API 본격 연동, 이후 PostgreSQL(TypeORM) 이관.
- 토글(지출 분류·과목 등) 관리자 추가 → **옵션 세트(룩업) 테이블** 도입 제안.
- 수업 피드백 → 학부모 카카오 발송(MQ) 연결.

## 4. 비고

- 데모 실행: `cd backend && npm run dev`(:3001, 문서 /docs) + `cd frontend && npm run dev`(:3000).
- 저장소 3개 모두 상세 커밋 이력 유지.

---

# TACO ERP: TBO-02

작성일: 2026-06-29 (월) · 작성: 개발자 (김민수)

> 목표: 아래의 TODO-List를 구현하는 것을 목표로 하나, Front의 UI/UX부터 구조화 및 최적화 하는 것을 목표로 함. 오늘부터는 개발이 어느정도 완료된 기능의 test-code와 상세 커밋, join시 관계 의존성 체크 및 강건성 재고, Backend와의 호환 및 확장 가능성을 목표함.

## 1. 상세 페이지

> 목록이 있는 탭에서 목록 안의 데이터가 많아지면 어떻게 최적화 및 UX 개선을 할 것인지 계획 필요함. page button, filtering, searching 등

> 모든 탭에서 캘린더, 신청 폼, 목록 등이 함께 있는데 조금 더 UI/UX적으로 예뻤으면 좋겠음. 캘린더는 일정 관련해서 페이지 예쁘게, 신청 폼은 신청 폼대로, 목록과 관리는 그것들 끼리 모아서 보여줘야함. (대신 상세한 정보가 한 페이지에 한 주제로 들어가게)
> 슬슬 front가 커지면서 Join이 많아지고 있음. 모든 작업에서 join 유효성 검사 -> 업데이트 모두 반영 되는지 체크 하고, 문서화를 꼼꼼하게 하는 것을 목표로 함. (백엔드와의 연동이 쉽게, .dbml도 틈틈이 수정해야 함.)

### 상담 탭

1. 상담 신청 폼을 상세 페이지로 빼면 좋겠음. (디자인을 더 예쁘게 설문지처럼 만들기 위함)
2. 상담탭에서는 대략적인 스케줄과 클릭시 상세 상담 및 예약 내역, 예약 신청, 스케줄 변경 등을 할 수 있도록 유도
3. 캘린더와 상담카드 목록 등에서 학생을 검색하거나, 상담자 이름을 필터링 하는 등의 기능이 있으면 좋겠음. (목록은 백엔드에서 요청할 수 있도록)

### 캘린더 탭

1. 월간 데이터만 보여주고 있으나, 주간 데이터도 볼 수 있어야 함. (구글 캘린더처럼 주별 시간별.)
2. 수정하기 기능을 추가하여 실제 캘린더처럼 커스텀 반복(~이후로 반복, ~까지 반복, 무슨 요일만 반복 등)과 시간 수정이 가능해야 함
   (ex: 월요일 12시부터 14시까지였던 수업을 월요일 14시부터 16시로 옮김 -> 관련 조인 계정 전부 업데이트 -> 반복 여부 물어봄 -> PNG 파일로 다운로드 가능 -> URL로 공유하기 가능)
3. 드래그를 통한 이동, 클릭하여 해당 스케줄을 모달로 띄운 뒤 시간 및 반복 횟수 등 상세 수정 가능하게

### 학생 부모 탭

1. 학생 목록에서 학생 정보 수정 기능이 필요함 (추후 Web ID 추가하기 위함)
2. 학생을 클릭했을 때 상세 페이지가 뜨면 좋겠음. (등록한 코스 -> 클릭시 강의 상세 페이지 -> 강사 상세 페이지 등 유기적인 원형 flow, 학생 상담 카드, 연락처, 특이 사항 메모 등도 함께 보여주면 좋을 듯)
3. 우리의 목적은 학생의 정보를 데이터화 하는 것임. 학생을 삭제하더라도, 상담 내역, 유저 정보, 학점 등은 삭제하면 안됨. 그저 관련된 스케줄 및 Join에서만 삭제해주면 됨 (status != drop 이런 식으로 필터링)

### 수업 강사 탭

1. 강사는 수업 개설 화면이 보이지 않아야 함.
2. 수업 목록에서 캘린더 뷰, 위클리 뷰도 추가해야 함
3. 강사를 위한 탭이므로, 내가 이번 달 몇번 수업을 했고, 얼마를 벌었는지 볼 수 있어야 함.
4. 테이블 컬럼에 진행 회차(12 / 15), 학생 수 등을 보여주면 좋겠음.

### 결제 수납 탭

> 대표님께서 가장 먼저 구현하라고 한 내용임. 꼭 완성될 필요는 없지만 일단 테스트라도 가능하게 front, back test-code + 실제 연결해서 backend console + test.txt에 저장해보는 식으로 구현해보기

1. 결제 수납에서 상태, 금액에 따른 시각화 뷰가 추가되면 좋겠음.
2. 기한/수납일 보다는 등록/수납일로 수업 등록과 수납을 언제 했는지 볼 수 있으면 좋을 것 같음.
3. 해당 탭은 아주 민감한 기능이므로 권한과 보안에 대해 아주 상세히 신경써야 함.

### 강사 페이 탭

1. DB join하여 해당 강사가 어떤 수업을 맡고 있는지 떠야 함
2. 해당 수업을 클릭하면 진행 회차, 시수당 금액 등이 자동으로 계산 됨
3. 다만 강사가 직접 수정을 할 수 있음.
4. join하여 기간, 강사의 코스 목록, 학생 목록, 스케줄 등을 아주 면밀히 연동해야 함 (절대 오류가 있으면 안됨.)

### 지출 비품 탭

1. 캘린더 뷰에서 모든 지출목록이 같은 색임.
2. 지출 분류에 따라 다른 색상으로 표현해야 함
3. 지출 등록이 있으나, 상세로 들어갔을 때 수정도 가능해야 함.

### 수업보고서

> TODO

### 관리자

1. 우선은 결제 및 수납 관련한 기능만
2. 결제 및 수납 체크, 지출 및 비품 승인 등 모든 승인 관련 권한 체크
3. 모든 승인 관련 join과 의존성 체크 후 test-code 작성
4. 개요, 캘린더 뷰에서 날짜를 클릭하면 스케줄 추가 및 수정이 가능해야 함. (이미 존재하는 스케줄도 클릭시 수정 및 삭제 가능)
5. 캘린더 탭에서 요청한 것 처럼 위클리 뷰도 추가 및 수정 삭제가 가능해야 함. (부모님은 위클리 뷰로 보는 것을 선호하기 때문임)
6. 코스추가, 과목 추가, 로드맵 추가가 모두 상세 페이지로 있어야 함. (우선은 지금의 정보로만 구성을 하되, 모달 등을 이용하여 예쁜 UI/UX로 구성해야 함.) -> 추후에 많은 옵션들이 들어갈 예정이라 그럼
7. 로드맵은 코스, 과목, 강사 등이 M:N으로 들어가므로 아주 면밀한 의존성 체크가 필요함. test-code를 작성해야 함
8. 이벤트 발행을 따로 빼기 보다는 캘린더 뷰에서 추가하면 좋을 것 같음. (모달로 띄우던가 상세 페이지를 만들던가)
9. 현재 승인 버튼을 눌러 여러가지의 승인을 한번에 보여주는데, 결국 관리자 페이지가 필요할 것 같음. 일단 로직부터 구현해놓고 추후에 관리자 페이지는 확장 및 분리하는 것으로 결정

---

# TACO ERP: TBO-03

작성일: 2026-06-29 (월) · 작성: 개발자 (김민수)

> 목표(대표님 신규 우선순위): **Lantiv형 스케줄 관리**를 최우선으로. 스케줄이 시수·회계의 단일 소스가 되도록 모델·엔진·뷰·테스트를 먼저 설계·문서화한 뒤 구현한다.
> 상세 설계: `docs/scheduling.md` (Lantiv Scheduling Studio 분석 기반)

## 1. 요구사항 (대표님 지시)

1. Lantiv 같은 스케줄 관리가 우선 (시수 체크 등 회계 처리의 전제).
2. **주간 뷰**: 색상·라벨(강사·학생·과목·과목종류)별 구분 + 전체 스케줄 + 기간 설정.
3. **일간 뷰**: 색/라벨 구분 동일 + **강의실별**로 보기(강의실 겹침 방지).
4. 학생·강사·**강의실** 리스트 → 클릭 시 예약 내역·스케줄 확인.
5. 검색: 텍스트 입력 + 라벨 선택 버튼 + 토글 + 기간 선택 등 다양한 옵션.
6. 학생·강사가 **각자 스케줄표(가용시간)**를 가짐.
   6-1. **불가시간(Block) 별도 설정** — 자원(학생/강사/강의실)별 차단 시간을 따로 지정. 자원 색과 무관하게 **고정 회색/검정**으로 통일 표시(차단 즉시 식별). → 슬롯 추천 제외 + 배치 시 충돌.
7. 6에 따라 **학생가용 ∧ 강사가용 → 주별 가능시간 자동 추천**(수업 할당 편의).
8. 다중 참조(기간별 시수·학생별 수업·리포트) → **무결성 필수 + 단위·참조 크기별 test-code**(추후 개발/확장 대비).
9. 계획 → TODO/문서화 먼저 실행.

## 2. 설계 요약 (→ docs/scheduling.md)

- 신규 모델: **Room**(capacity·색·건물계층), **AvailabilityBlock**(학생/강사/강의실 가용·불가, 주간반복+기간), **ClassSession 확장**(`roomId`·`startTime/endTime`·`seriesId`).
- 엔진(순수함수 `lib/domain/schedule.ts`, 백엔드 ScheduleService와 1:1): `overlaps` / `detectConflicts` / `suggestSlots` / `teachingHours`.
- 충돌(Lantiv 기반 적용분): 강사·강의실·학생 **이중예약**, **불가시간**, 강의실 **capacity**. (이동거리·장비·교시 등은 보류, enum 자리만)
- 뷰: 주간(색/라벨/기간 필터) · 일간(강의실 컬럼·겹침 시각화) · 자원 리스트+개인 스케줄 · 공통 검색·필터 바.
- 시수/회계: held 세션 → 기간·강사·학생별 집계 → 강사 페이(payroll) 통합.

## 3. 단계 (우선순위) — 진행 현황 (2026-06-29 기준)

- `0` 문서화 — ✅ **완료**: `docs/scheduling.md`, ERD(rooms·availability·room_id), OpenAPI.
- `1` 계약 + 엔진 + Vitest 단위·스케일 테스트 — ✅ **완료**: contracts(Room·AvailabilityBlock·ScheduleRow·Conflict·roomId/endTime/seriesId·RecurrenceScope), `lib/domain/schedule.ts`(overlaps·detectConflicts·teachingHours·move/resize·**suggestSlots·layoutLanes**), **Vitest 30 pass(N=10/100/1000 포함)**.
- `2` 주간 뷰 — ✅ **완료**: `/timetable`(표·엑셀/CSV) + `/calendar`(주간 그리드).
- `3` 일간(강의실) 뷰 — ✅ **완료**: `/calendar` 일간(강의실 컬럼).
- `3.5` 인터랙션 — ✅ 드래그 이동·시작/끝 리사이즈·클릭 상세 모달 + 백엔드 `PATCH /schedule/:id`(FK 검증 + 충돌 409/force) + **겹침 레인 표시(layoutLanes)**. ⚠️ 브라우저 인터랙션 QA 남음.
- `3.6` 반복 시리즈 편집(scope) — ✅ **엔진·API 완료**: `PATCH`에 `scope`(this/this_and_following/all) → 같은 seriesId에 날짜·시간 델타 동반 적용, 시드도 주간반복 시리즈 단위. 캘린더 일원화(`/schedule`→`/calendar` redirect). ⚠️ UI 연동·QA 남음.
- `5(엔진)` **슬롯 추천 `suggestSlots`** — ✅ **완료**: 가용 ∩ − 점유(강사/강의실 점유·불가 Block 제외) → 주별 후보 + Vitest 2종.
- `4` 자원 리스트/개인 스케줄 — ✅ **완료**: 좌측 자원 레일(강사/학생/강의실) `GET /schedule/resources` → 선택 시 서버 필터(instructorId|roomId|studentId) 개인 스케줄 + 요약.
- `5(UI)` 가용시간 입력 + 추천 화면 — ✅ **완료**: 가용·불가 CRUD(`PUT/DELETE /availability`) + **학생 중심 추천**(`recommendForStudent`, 불가 강사 주황 "조정 배정") + 강사∧학생 가용(`suggestPairSlots`) → `POST /schedule` 배정.
- `6` 시수/회계 연결 + 충돌 경고 UX — 🟡 **부분**: 충돌 경고 UX ✅ / 시수 집계(엔진·표·헤더 표시) ✅ / **강사 페이(payroll) 연결 남음**.
- `7` 백엔드 모듈 + e2e — ✅ **완료**: rooms·availability·schedule 모듈 + `GET /schedule/resources`·`POST /schedule`·studentId 필터 + **jest+supertest e2e**(schedule·availability·참조무결성).

### 캘린더 탭 통합 (2026-06-29, 대표님 신규 요청)

- `/calendar`로 **월간·주간·일간(강의실)·표** 통합. `/timetable`·`/schedule` redirect, 사이드바 "주간 표" 제거(주간 표=「표」 뷰로 흡수, 엑셀/CSV·시수).
- 색상/필터/라벨에 **학생** 차원 추가(`ScheduleRow.studentIds/Names`, 코호트 status≠drop).
- 불가시간(Block) **캘린더 밴드(회색 사선)** 표시.
- 보안 점검: `backend/docs/SECURITY-review-2026-06-29.md`.

### 남은 작업 계획 (다음 스프린트)

1. (6) **시수 → 강사 페이 연결** — `teachingHours`를 payroll 산정 입력으로 통합.
2. 보완 — 가용 'available' 밴드(초록) 표시 옵션, 반복 편집 UI("이후 전부") 연동, 드래그 브라우저 QA, OpenAPI/`scheduling.md`/contracts 동기 유지.

> 상세 점검 스냅샷: `docs/TBO-03_진행점검_2026-06-29.md` (코드 실측 기준)

## 4. 테스트 (요구 #8 — 필수)

- 단위: `overlaps` 경계값, `detectConflicts` 유형별(강사/강의실/학생/불가/capacity), `suggestSlots`, `teachingHours`.
- 참조 크기별: N=10/100/1000 픽스처로 불변식(시간겹침 0·FK 존재·시수합 일치) + 수행시간 상한(회귀 방지).
- 통합 무결성: room/availability FK, 퇴원(소프트삭제) 학생은 활성 스케줄 스코프에서 제외.

## 5. 보류 (추후 — 모델 확장 여지만)

그룹/하위그룹(우리는 course 코호트로 대체), 장비, 강의실 이동거리 충돌, 교시(Period) 시간축, 격주/제네릭일, 선택과목 미등록 충돌, CSV 임포트, 실시간 다중 협업.

---

# TACO ERP: TBO-04

작성일: 2026-06-30 (화) · 작성: 개발자 (김민수)

> 목표: 캘린더/스케줄 UX 고도화 — **확장성(자원 多)·관리자 직접 추가·추천 강화·불가시간 시각화**.
> TBO-03(캘린더 통합 + Lantiv 추천) 위에서 사용성·실무 적합성을 끌어올린다. 코드 수정 전 계획·TODO부터.

## 1. 요구사항 (지시)

1. **상태 필터 제거** — 강사·과목·학생·강의실별 구분은 유지하되, **상태(예정/진행 등)는 필터에서 뺀다**(상세에서만 확인).
2. **자원 탐색 확장성** — 강사·학생이 많아지면 지금의 칩 UI로는 찾기 어려움. Lantiv처럼 **별도 모달** 또는 **우측 공간에 검색 + 페이지네이션 컴포넌트**를 따로 만들어 렌더링.
3. **시작–종료 시각 모두 표시** — 일정 블록에 `시작–끝`을 함께(현재 시작만 보이는 곳 보완).
4. **관리자 직접 추가** — 캘린더 탭에서 **관리자(super_admin/admin)에게 "스케줄 추가하기"** 노출(직접 세션 생성).
5. **추천 강화(학생 → 빈 시간의 가용 강사)** — 학생 스케줄을 보면 그 **빈 시간에 가능한 강사를 추천**(슬롯 단위로 가용 강사 리스트).
6. **불가시간 블락 시각화** — 안 되는 시간을 캘린더에서 **블록으로 칠하고 설정**(가용/불가 구분 + 드래그 설정).

## 2. 설계 요약

- **(1) 상태 필터 제거**: `ScheduleCalendar`의 `fStatuses` 칩·로직 제거. 상태는 상세 모달/색 옵션에만 잔존.
- **(2) 자원 필터 패널**: 신규 `ResourceFilterPanel`(우측). 탭(강사·학생·강의실·과목) + **검색 input** + **페이지네이션**(또는 가상 스크롤) + 다중 체크. 선택값은 기존 `filtered` 로직 재사용. 좁은 화면은 버튼→모달 대체.
  - 소수일 땐 요약 칩, 다수일 땐 패널로 — **칩은 "선택됨 요약"만, 추가/탐색은 패널**로 분리.
- **(3) 블록 라벨**: 주/일 그리드·표·월간 블록에 `HH:mm–HH:mm` 표기. 칸이 좁으면 시작만 + hover 전체(title) 폴백.
- **(4) 관리자 추가**: 데모 역할(현재 사이드바 `super_admin`)을 **역할 컨텍스트**로 끌어와 `admin|super_admin`일 때 "+ 스케줄 추가" 버튼/날짜 클릭 → 생성 모달(course·instructor·room·date·start·end) → `POST /schedule`(기존). 빈 슬롯/날짜 프리필.
- **(5) 추천 엔진 확장**: `freeSlotsForResource(student)`(가용−불가−점유 → 빈 구간) + `availableInstructorsAt(date,start,end)`(그 시간 가용∧비점유∧비불가 강사). 추천 패널을 **"빈 슬롯 → 가능 강사 N명"** 구조로 재구성(`recommendForStudent` 보강/대체).
- **(6) 불가시간 인터랙션**: 자원 선택 상태에서 **그리드 드래그 → unavailable Block 생성**(`PUT /availability`) + 가용=초록 밴드·불가=회색 밴드 **구분 표시** + 블록 클릭 삭제. (밴드 표시는 TBO-03에서 일부 구현 → 설정/드래그·가용 밴드 추가)

## 3. 단계 (우선순위) — 진행 현황 (2026-06-30 기준, 전부 ⛔ 착수 전)

- `A` (요구 1) 상태 필터 제거 — ⛔ **남음**(간단).
- `B` (요구 3) 블록 시작–종료 표시 — ⛔ **남음**(간단).
- `C` (요구 4) 관리자 "스케줄 추가" 모달 + 역할 게이팅 — ⛔ **남음**. (`POST /schedule`은 존재 → UI·권한만)
- `D` (요구 2) 자원 검색·페이지네이션 필터 패널 — ⛔ **남음**(핵심 UX).
- `E` (요구 5) 추천 고도화: 빈 슬롯 → 가용 강사 — ⛔ **남음**(엔진 함수 + 추천 패널 개편).
- `F` (요구 6) 불가시간 드래그 블락 + 가용/불가 시각 구분 — ⛔ **남음**.

> 권장 순서: A·B(빠른 개선) → C(관리자 추가) → D(확장성) → E(추천) → F(블락). A·B·C는 즉시 효과, D·E·F는 설계 비중 큼.

## 4. 테스트 (요구 #8 기조 유지)

- 엔진 단위: `freeSlotsForResource`(경계·연속 점유 병합), `availableInstructorsAt`(가용∧비점유∧비불가), 기존 `recommendForStudent`와 정합.
- 스케일: 강사 N(10/50)·학생 N에서 패널 페이지네이션·추천 성능 상한.
- e2e: 관리자 `POST /schedule`(권한 자리 포함), `availability` 블락 CRUD ↔ 충돌 연동(기존 15 + 추가).

## 5. 결정 (확정 2026-06-30)

- (2) 자원 필터 형태 → ✅ **우측 상시 패널**(탭 강사·학생·강의실·과목 + 검색 + 페이지네이션, 선택 요약 칩은 상단).
- (4) 관리자 구분 → ✅ **데모 역할 컨텍스트**(현재 super_admin) — `admin|super_admin`일 때 "스케줄 추가" 노출. 추후 JWT roles 연동 시 교체.
- 진행 순서 → ✅ **A·B·C 먼저** → D → E → F.
- (5) 추천 표시(슬롯 정렬·0명 슬롯 노출), (6) 불가시간 생성 단위/색 — D 이후 착수 시 확정.

## 6. 보류

월간 뷰의 시작–종료 동시표기(공간 제약 시 시작만), 다중 자원 동시 블락, 실시간 충돌 알림.
